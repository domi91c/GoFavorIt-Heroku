<p id="notice"><%= notice %></p>

<p>
  <strong>Bio:</strong>
  <%= @wall.bio %>
</p>

<p>
  <strong>User:</strong>
  <%= @wall.user_id %>
</p>

<%= link_to 'Edit', edit_wall_path(@wall) %> |
<%= link_to 'Back', walls_path %>




<div class="row">

  <ul class="thumbnails files">
    <% @wall.gallery.pictures.each do |pic| %>
        <li class="span3" id="picture_<%= pic.id %>">
          <div class="thumbnail">
            <%= image_tag pic.image.url %>
            <div class="caption">
              <p style="text-align: center;">
                <%= link_to "Edit", picture_path(pic) %>

                <%=
                    link_to "Delete", pic,
                            :data => { :confirm => "Are you sure?" },
                            :method => :delete
                %>

              </p>
            </div>
          </div>
        </li>
    <% end %>
  </ul>
</div>